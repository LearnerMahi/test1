name: AndroidBuild
on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
jobs:
  build:
    runs-on:
      windows
    steps:
      - name: Checkout
        uses: actions/checkout@v4.1.5